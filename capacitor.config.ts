import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'easy.culture.guamote',
  appName: 'Easy Culture Guamote',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
